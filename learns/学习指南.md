# Moltbot 代码库学习指南

## 一、项目概述

**Moltbot** 是一个成熟的本地优先个人 AI 助手平台，核心功能是作为多渠道消息网关，将各种消息平台（WhatsApp、Telegram、Slack、Discord、Signal、iMessage 等 12+ 个）与 AI 模型（Claude、ChatGPT 等）连接起来。

### 核心特性
- 🔌 多渠道消息集成（12+ 平台）
- 🤖 AI 模型对接（支持多供应商）
- 🛠️ 工具系统（浏览器自动化、Canvas 渲染、技能系统）
- 🖥️ 跨平台支持（Web、macOS、iOS、Android、Linux、Windows/WSL）
- 🔒 安全机制（设备认证、权限模型、DM 配对）

### 技术栈
| 类别 | 技术 |
|------|------|
| 语言 | TypeScript (ESM, 严格模式) |
| 运行时 | Node.js ≥22.12.0 |
| 包管理 | pnpm v10.23.0 |
| 测试 | Vitest |
| 代码质量 | oxlint, oxfmt |

---

## 二、架构概览

```
消息渠道 (WhatsApp, Telegram, Slack, Discord, Signal, iMessage, 等)
                    ↓
        ┌─────────────────────────────┐
        │    Gateway (WebSocket)      │
        │   (控制平面 @ :18789)        │
        └──────────────┬──────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ↓              ↓              ↓
    Pi Agent        CLI 工具       WebChat UI
    (RPC 模式)   (moltbot ...)    (浏览器)

        加上: macOS App, iOS/Android Nodes, Canvas Host
```

**核心设计理念**：一切都通过 **Gateway** 流转——这是一个基于 WebSocket 的控制平面，管理会话、渠道、定时任务、Webhook 和客户端连接。

---

## 三、目录结构详解

```
moltbot/
├── src/                          # 主 TypeScript 源码
│   ├── agents/                   # Pi agent 运行时 (RPC 模式)
│   │   ├── pi-embedded-runner/   # 核心 agent 执行
│   │   ├── pi-embedded-helpers/  # Agent 工具函数
│   │   ├── auth-profiles/        # 多配置认证 (OAuth, API keys)
│   │   ├── tools/                # Agent 工具 (bash, browser, canvas, nodes)
│   │   ├── skills/               # 工作区技能系统
│   │   └── sandbox/              # Docker 沙箱隔离
│   │
│   ├── gateway/                  # WebSocket 网关服务器
│   │   ├── server.impl.ts        # 核心服务器实现
│   │   ├── client.ts             # 网关客户端
│   │   ├── session-utils.ts      # 会话管理
│   │   ├── call.ts               # 调用处理
│   │   ├── hooks.ts              # Hook 系统
│   │   └── protocol/             # 网关协议定义
│   │
│   ├── channels/                 # 消息集成
│   │   ├── allowlists/           # 渠道白名单逻辑
│   │   ├── plugins/              # 渠道插件 (动作, 路由)
│   │   └── *.ts                  # 各渠道实现
│   │
│   ├── cli/                      # CLI 连接层
│   │   ├── gateway-cli/          # Gateway 命令
│   │   ├── daemon-cli/           # 守护进程管理
│   │   └── program/              # 命令路由
│   │
│   ├── commands/                 # 命令实现
│   │   ├── agent.ts              # Agent (聊天) 命令
│   │   ├── health.ts             # 健康检查命令
│   │   └── onboard*.ts           # 入门向导
│   │
│   ├── config/                   # 配置系统
│   ├── providers/                # 模型供应商
│   ├── browser/                  # 浏览器自动化
│   ├── media/                    # 媒体处理管道
│   ├── infra/                    # 基础设施工具
│   ├── security/                 # 安全工具
│   └── sessions/                 # 会话存储
│
├── skills/                       # 内置技能 (50+ 个)
├── extensions/                   # 插件扩展
├── apps/                         # 原生应用
│   ├── macos/                    # macOS 菜单栏应用
│   ├── ios/                      # iOS 应用
│   └── android/                  # Android 应用
├── ui/                           # Web UI
├── docs/                         # 文档 (Mintlify)
└── test/                         # 测试工具
```

---

## 四、核心模块详解

### 4.1 Gateway（网关）
**位置**: `src/gateway/`

系统的"中枢神经"，是一个 WebSocket 服务器：
- 管理客户端连接（CLI、WebChat、macOS 应用、iOS/Android 节点）
- 处理会话生命周期（创建、恢复、重置、保存、加载）
- 路由入站消息从渠道到 Agent
- 分发出站消息回渠道
- 执行定时任务和 Webhook

**核心文件**:
- `server.impl.ts` - 主服务器逻辑（⭐ 必读）
- `client.ts` - 网关客户端
- `session-utils.ts` - 会话持久化和管理
- `protocol/` - 消息帧和验证

### 4.2 Agents（代理）
**位置**: `src/agents/`

Pi agent 运行时，执行 AI 任务：
- `pi-embedded-runner/` - 运行 Claude/OpenAI 模型
- `auth-profiles/` - 多配置认证（OAuth 轮换、API key 降级）
- `tools/` - 文件 I/O、Canvas 控制、节点调用、消息
- `skills/` - 工作区技能集成
- `sandbox/` - Docker 隔离执行不可信代码

### 4.3 Channels（渠道）
**位置**: `src/channels/` + 各独立模块

集成 12+ 消息平台：

| 渠道 | 模块 | 使用库 |
|------|------|--------|
| WhatsApp | `src/whatsapp/` | @whiskeysockets/baileys |
| Telegram | `src/telegram/` | grammy |
| Slack | `src/slack/` | @slack/bolt |
| Discord | `src/discord/` | discord.js |
| Signal | `src/signal/` | signal-cli (子进程) |
| iMessage | `src/imessage/` | imsg (macOS 桥接) |

### 4.4 CLI（命令行）
**位置**: `src/cli/` + `src/commands/`

50+ 个命令，核心包括：
- `moltbot gateway` - 启动网关
- `moltbot agent` - 与 Agent 聊天
- `moltbot onboard` - 交互式设置向导
- `moltbot doctor` - 诊断和状态迁移
- `moltbot channels` - 渠道管理
- `moltbot skills` - 技能管理

### 4.5 Skills（技能）
**位置**: `skills/`

50+ 个内置技能：
- **生产力**: Apple Notes, Notion, Obsidian, Trello
- **集成**: GitHub, Slack, Discord, Spotify
- **工具**: 天气, 定时器, 截图
- **AI**: Whisper 语音, 图像生成, 编码代理

---

## 五、关键设计模式

### 5.1 依赖注入 (DI)
```typescript
async function doSomething(opts: {}, deps: {
  gateway: GatewayClient;
  config: Config;
  logger: Logger;
}) { }
```
通过 `createDefaultDeps()` 创建依赖，便于测试和解耦。

### 5.2 流式响应
Agent 响应以块（文本、工具调用、推理）流式返回，而非完整消息。

### 5.3 会话模型
- `main`: 直接 1-on-1 会话
- 群组隔离: 每群组独立会话
- 激活模式: `mention` (提及回复) vs `always` (始终响应)

### 5.4 认证配置轮换
多个认证配置（API keys、OAuth tokens）自动故障转移和冷却跟踪。

---

## 六、建议学习路径

### 阶段 1: 入门（1-2 天）
**目标**: 理解项目做什么，能运行起来

1. **阅读文档**
   - `README.md` - 项目概述
   - `AGENTS.md` - 团队指南和架构
   - `docs/concepts/architecture.md` - Gateway 设计

2. **运行项目**
   ```bash
   pnpm install
   pnpm dev
   pnpm moltbot --help
   ```

3. **体验功能**
   ```bash
   pnpm moltbot onboard        # 交互式设置
   pnpm moltbot agent          # 与 Agent 对话
   ```

### 阶段 2: 核心系统（3-5 天）
**目标**: 理解核心架构

1. **Gateway 系统**（⭐ 最重要）
   - 阅读 `src/gateway/server.impl.ts`
   - 理解 WebSocket 消息流
   - 跟踪一个消息从接收到响应的完整流程

2. **Agent 执行**
   - 阅读 `src/agents/pi-embedded-runner/`
   - 理解工具调用机制
   - 查看 `src/agents/tools/` 中的工具实现

3. **渠道系统**
   - 选择一个熟悉的渠道（推荐 Telegram 或 Discord）
   - 阅读对应的 `src/telegram/` 或 `src/discord/`
   - 理解消息接收和发送流程

### 阶段 3: CLI 和配置（2-3 天）
**目标**: 理解用户交互层

1. **命令路由**
   - `src/cli/program/` - 命令注册和路由
   - `src/commands/agent.ts` - 聊天命令实现

2. **配置系统**
   - `src/config/config.ts` - 配置加载和验证
   - 理解 Zod schema 验证

### 阶段 4: 进阶主题（5-7 天）
**目标**: 深入特定领域

1. **认证系统**
   - `src/agents/auth-profiles/` - 认证故障转移

2. **安全机制**
   - `src/security/` - DM 配对、权限模型

3. **媒体处理**
   - `src/media/` - 图像理解、链接解析

4. **技能系统**
   - `skills/` - 技能结构和开发

### 阶段 5: 扩展开发（持续）
**目标**: 能够贡献代码

1. **插件开发**
   - 阅读 `docs/concepts/plugins.md`
   - 查看 `extensions/` 中的示例

2. **渠道开发**
   - 参考现有渠道实现新渠道

3. **工具开发**
   - 在 `src/agents/tools/` 添加新工具

---

## 七、代码阅读技巧

### 7.1 从入口开始
```
moltbot.mjs → src/index.ts → src/cli/program/ → 具体命令
```

### 7.2 跟踪消息流
```
渠道接收消息 → channels/registry → gateway/server.impl
→ agents/pi-embedded-runner → 工具执行 → 响应返回
```

### 7.3 使用测试理解代码
- 单元测试 `*.test.ts` 展示函数用法
- E2E 测试 `*.e2e.test.ts` 展示完整流程

### 7.4 善用类型定义
- 查看 Zod schemas 理解数据结构
- 查看接口定义理解模块契约

---

## 八、重要文件索引

| 优先级 | 文件 | 说明 |
|--------|------|------|
| ⭐⭐⭐ | `src/gateway/server.impl.ts` | 网关核心，系统心脏 |
| ⭐⭐⭐ | `src/agents/pi-embedded-runner/` | Agent 执行引擎 |
| ⭐⭐ | `src/channels/registry.ts` | 渠道生命周期管理 |
| ⭐⭐ | `src/config/config.ts` | 配置系统 |
| ⭐⭐ | `src/commands/agent.ts` | 聊天命令实现 |
| ⭐ | `src/cli/program/` | CLI 命令路由 |
| ⭐ | `src/agents/tools/` | 工具实现 |
| ⭐ | `src/infra/` | 基础设施工具 |

---

## 九、常用开发命令

```bash
# 开发
pnpm dev                    # 开发模式运行
pnpm gateway:watch          # 自动重载网关

# 测试
pnpm test                   # 运行所有测试
pnpm test:coverage          # 覆盖率报告

# 构建
pnpm build                  # 编译 TypeScript
pnpm ui:build               # 构建 Web UI

# 代码质量
pnpm lint                   # 代码检查
pnpm format:fix             # 格式化
```

---

## 十、学习资源

### 内部资源
- `docs/` - 官方文档（100+ 页）
- `AGENTS.md` - 团队开发指南
- `CHANGELOG.md` - 版本变更记录

### 外部依赖文档
- [grammy](https://grammy.dev/) - Telegram Bot API
- [discord.js](https://discord.js.org/) - Discord API
- [@slack/bolt](https://slack.dev/bolt-js/) - Slack API
- [Playwright](https://playwright.dev/) - 浏览器自动化
- [Zod](https://zod.dev/) - Schema 验证

---

## 十一、学习建议

1. **不要试图一次性理解全部** - 项目有 2500+ 文件，50万+ 行代码
2. **从你感兴趣的部分开始** - 对某个渠道感兴趣就先看那个
3. **动手修改和调试** - 实践是最好的学习方式
4. **善用 IDE 的跳转功能** - 快速定位定义和引用
5. **阅读测试用例** - 测试往往是最好的文档
6. **关注架构而非细节** - 先理解整体设计，再深入具体实现

---

*文档生成时间: 2026-01-31*
*代码库版本: 2026.1.29*
